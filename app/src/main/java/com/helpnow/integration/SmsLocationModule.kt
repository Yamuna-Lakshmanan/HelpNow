package com.helpnow.integration

/**
 * Integration point for Module 3 (SMS + Location).
 * Replace with Yamuna's implementation when Module 3 is available.
 */
interface SmsLocationModule {
    fun sendEmergencyAlert()
}
